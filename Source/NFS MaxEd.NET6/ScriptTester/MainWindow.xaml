<Window
    AllowsTransparency="False"
    Background="{StaticResource BackgroundDark}"
    Height="550"
    MinHeight="400"
    MinWidth="600"
    Title="Script Tester"
    Width="950"
    WindowStyle="None"
    x:Class="ScriptTester.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="40"
            CornerRadius="0"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource BackgroundMedium}" Grid.Row="0">
            <TextBlock
                FontSize="12"
                Foreground="Gray"
                Margin="15,0,0,0"
                Text="Script Tester"
                VerticalAlignment="Center" />

            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Click="MinimizeBtn_Click"
                    Content="━"
                    Style="{StaticResource WindowButtonStyle}"
                    WindowChrome.IsHitTestVisibleInChrome="True" />
                <Button
                    Click="MaximizeBtn_Click"
                    Content="▢"
                    Style="{StaticResource WindowButtonStyle}"
                    WindowChrome.IsHitTestVisibleInChrome="True" />
                <Button
                    Click="CloseBtn_Click"
                    Content="×"
                    Style="{StaticResource CloseWindowButtonStyle}"
                    WindowChrome.IsHitTestVisibleInChrome="True" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <Button
                    Click="SelectFiles_Click"
                    Content="Выбрать файлы"
                    DockPanel.Dock="Top"
                    Height="60"
                    Margin="0,0,0,10"
                    Style="{StaticResource ButtonStyle}" />

                <ListBox SelectionChanged="PairsListBox_SelectionChanged" x:Name="PairsListBox">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    FontWeight="Bold"
                                    Foreground="{Binding StatusColor}"
                                    Margin="0,0,10,0"
                                    Text="{Binding StatusIcon}" />
                                <TextBlock Foreground="White" Text="{Binding FileName}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <GridSplitter
                Background="Transparent"
                Grid.Column="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Stretch"
                Width="5" />

            <Grid Grid.Column="2" Margin="10,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <UniformGrid
                    Columns="2"
                    Grid.Row="0"
                    Margin="0,0,0,5">
                    <TextBlock
                        FontSize="10"
                        Foreground="Gray"
                        Text="Golden: -"
                        TextTrimming="CharacterEllipsis"
                        x:Name="GoldenLabel" />
                    <TextBlock
                        FontSize="10"
                        Foreground="Gray"
                        Margin="5,0,0,0"
                        Text="Test: -"
                        TextTrimming="CharacterEllipsis"
                        x:Name="TestLabel" />
                </UniformGrid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ListBox
                        Background="{StaticResource BackgroundLight}"
                        FontFamily="Consolas"
                        Grid.Column="0"
                        x:Name="GoldenLinesList" />
                    <ListBox
                        Background="{StaticResource BackgroundLight}"
                        FontFamily="Consolas"
                        Grid.Column="1"
                        Margin="5,0,0,0"
                        x:Name="TestLinesList" />
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>