<Page
    Background="Transparent"
    Title="RacerNamePage"
    mc:Ignorable="d"
    x:Class="Maximus.Views.LeadersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>
        <VisualBrush
            Stretch="Uniform"
            TileMode="Tile"
            Viewport="0,0,30,30"
            ViewportUnits="Absolute"
            x:Key="ArrowTileBrush">
            <VisualBrush.Visual>
                <Image Source="pack://application:,,,/Assets/STRELKA.png" Width="30">
                    <Image.RenderTransform>
                        <ScaleTransform CenterX="15" ScaleX="-1" />
                    </Image.RenderTransform>
                </Image>
            </VisualBrush.Visual>
        </VisualBrush>

        <Style TargetType="Grid" x:Key="CardGridStyle">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <Grid VerticalAlignment="Center">
        <StackPanel>
            <Border Margin="20" Style="{StaticResource CardStyle}">
                <Grid Style="{StaticResource CardGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Grid.Column="0"
                        IsEnabled="False"
                        Margin="5"
                        Text="RACENAME_000"
                        VerticalAlignment="Center" />

                    <Rectangle
                        Fill="{StaticResource ArrowTileBrush}"
                        Grid.Column="1"
                        Height="30"
                        Margin="10,0"
                        Width="210" />

                    <TextBox
                        Grid.Column="2"
                        Margin="5"
                        VerticalAlignment="Center" />
                </Grid>
            </Border>

            <Border Margin="20" Style="{StaticResource CardStyle}">
                <Grid Style="{StaticResource CardGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        IsEnabled="False"
                        Margin="5"
                        Text="RACENAME_001"
                        VerticalAlignment="Center" />
                    <Rectangle
                        Fill="{StaticResource ArrowTileBrush}"
                        Grid.Column="1"
                        Height="30"
                        Margin="10,0"
                        Width="210" />
                    <TextBox
                        Grid.Column="2"
                        Margin="5"
                        VerticalAlignment="Center" />
                </Grid>
            </Border>
            <Border Margin="20" Style="{StaticResource CardStyle}">
                <Grid Style="{StaticResource CardGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        IsEnabled="False"
                        Margin="5"
                        Text="RACENAME_002"
                        VerticalAlignment="Center" />
                    <Rectangle
                        Fill="{StaticResource ArrowTileBrush}"
                        Grid.Column="1"
                        Height="30"
                        Margin="10,0"
                        Width="210" />
                    <TextBox
                        Grid.Column="2"
                        Margin="5"
                        VerticalAlignment="Center" />
                </Grid>
            </Border>
            <Border Margin="20" Style="{StaticResource CardStyle}">
                <Grid Style="{StaticResource CardGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        IsEnabled="False"
                        Margin="5"
                        Text="RACENAME_003"
                        VerticalAlignment="Center" />
                    <Rectangle
                        Fill="{StaticResource ArrowTileBrush}"
                        Grid.Column="1"
                        Height="30"
                        Margin="10,0"
                        Width="210" />
                    <TextBox
                        Grid.Column="2"
                        Margin="5"
                        VerticalAlignment="Center" />
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</Page>