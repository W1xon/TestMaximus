<Page
    Background="#1a1a1a"
    Title="MilestonesPage"
    mc:Ignorable="d"
    x:Class="Maximus.Views.MilestonesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Top Panel with rounded corners  -->

        <TextBlock
            FontSize="60"
            FontWeight="Bold"
            Foreground="White"
            HorizontalAlignment="Center"
            Text="Milestones Configuration" />

        <!--  Main Content  -->
        <ScrollViewer
            DataContext="{Binding MilestoneConfig}"
            Grid.Row="1"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Basic Information Section  -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock
                            FontSize="18"
                            FontWeight="Bold"
                            Margin="0,0,0,15"
                            Style="{StaticResource LabelStyle}"
                            Text="Basic Information" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--  Milestone Type  -->
                            <StackPanel Grid.Column="0">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Milestone Type" />
                                <ComboBox ItemsSource="{Binding MilestoneTypes}" SelectedItem="{Binding MilestoneType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  Spawn Point  -->
                            <StackPanel Grid.Column="2">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Spawn Point" />
                                <ComboBox ItemsSource="{Binding SpawnPoints}" SelectedItem="{Binding SelectedSpawnPoint, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <!--  Bin Index  -->
                            <StackPanel Grid.Column="4">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Bin Index" />
                                <ComboBox ItemsSource="{Binding BinIndices}" SelectedItem="{Binding SelectedBinIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!--  Numeric Values Section  -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock
                            FontSize="18"
                            FontWeight="Bold"
                            Margin="0,0,0,15"
                            Style="{StaticResource LabelStyle}"
                            Text="Numeric Parameters" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="15" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="15" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  Bounty  -->
                            <StackPanel Grid.Column="0" Grid.Row="0">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Bounty" />
                                <TextBox Text="{Binding Bounty, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  Goal Add Prev Best  -->
                            <StackPanel Grid.Column="2" Grid.Row="0">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Goal Add Prev Best" />
                                <TextBox Text="{Binding GoalAddPrevBest, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  Goal Easy  -->
                            <StackPanel Grid.Column="0" Grid.Row="2">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Goal Easy" />
                                <TextBox Text="{Binding GoalEasy, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>

                            <!--  Goal Hard  -->
                            <StackPanel Grid.Column="2" Grid.Row="2">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    Style="{StaticResource LabelStyle}"
                                    Text="Goal Hard" />
                                <TextBox Text="{Binding GoalHard, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>